# Laravel-Task-Management-System
A Laravel Task Management System that allows users to manage projects and tasks. Features include adding, editing, and deleting tasks and projects, filtering tasks by project, status, and priority, and marking tasks as completed. The system uses proper MVC structure, Laravel validation, resource routes, and clean, readable code.
